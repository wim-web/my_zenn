version: 1

imports:
  - path: ./node_modules/textlint-rule-preset-icsmedia/dict/prh.yml
    ignoreRules:
      - expected: VS Code
      - expected: プロバイダー
      - expected: コンテナー


rules:
  - expected: VSCode
    specs:
      - from: vscode
        to: VSCode
      - from: VScode
        to: VSCode
    prh: 大文字・小文字を確認してください
  - expected: Terraform Cloud
    specs:
      - from: terraform cloud
        to: Terraform Cloud
    prh: 大文字・小文字を確認してください
ignoreRules:
  - expected: プロバイダー

